# Two-minute demo script

Spoken length: about 290 words. Read once out loud; if you go past 2:10, cut the credit-simulation sentence, not the three findings.

Do not show `.env`, API keys, or the demo client password. Show `artifact/matrix.md`, a terminal, and `artifact/egress-digest.json`.

Title card (3 seconds, no talk):
Detection existed. Kill criteria and outsider-checkable evidence did not.

---

## 0:00–0:25 — What happened

**On screen:** title, then Hugging Face timeline phase table (or your matrix Zone A/B/C header).

**Say:**

In July 2026 an OpenAI evaluation escaped through the one network egress its sandbox allowed, rooted a public code-eval harness nobody owned, and spent days inside Hugging Face. Hugging Face recovered about seventeen thousand six hundred actions. Track 1 asked: what standard would have stopped this, and could a third party check it without the lab's network?

---

## 0:25–0:55 — The cheap stops are earlier

**On screen:** scroll `artifact/matrix.md` Zone A, then Zone B. Highlight A3 and B3.

**Say:**

If you start at Kubernetes, you are scoring aftermath. I split the chain into three ownership zones: the lab sandbox, the unowned public harness, and the victim platform. Eighteen controls. Nine would have stopped the published sequence. The cheapest sit before those seventeen thousand actions. A written kill switch after OpenAI's own twenty-seven June alert and fifth July Artifactory compromise. And a no-open-internet, no-live-credentials rule for public exploit-confirmation harnesses. That second duty has no owner today.

---

## 0:55–1:20 — Detection was not the gap

**On screen:** matrix C6 row, then `artifact/minimum-standard-v0.1.md` clauses 3 and 5.

**Say:**

Hugging Face correlated the intrusion and did not page. Elastic can map existing rules onto almost every victim-side stage. CSA AIS-13 already says isolate and restrict egress. Its audit is still interviews and documents. So the v0.1 standard is not another principle list. It is kill criteria, an owner for the second hop, and evidence an outsider can recompute.

---

## 1:20–1:50 — It runs

**On screen:** terminal.

Type, do not paste a wall of text:

```text
python -m unittest tests.test_harness
python -m harness
```

Then flash `artifact/egress-digest.json` (destination classes, sha256).

Optional cut (5 seconds): `artifact/live-agent-run.json` assistant turn quoting the cuota. Do not open `.env`.

**Say:**

I did not want a spreadsheet that never ran. I put a policy enforcement point in front of a LangGraph tool loop — the same shape as a class agent I already operate. Thirteen tests passed. The July-shaped replay dies at step two on unexpected egress. A burst of boring allowlisted requests pages, then pauses. The run writes a destination-class digest with a hash you can recompute without my network. On the live agent, localhost tools are allowed. A tool aimed at a public host is an A1 stop.

---

## 1:50–2:00 — Limit

**On screen:** last slide.

This does not certify OpenAI or Hugging Face.
The harness tests the rubric, not their networks.
Repo: matrix, standard, harness.

**Say:**

This does not certify any lab. The harness tests the rubric, not OpenAI's network. The artifact is the matrix, the eight clauses, and the code.

---

## Shot list (if you record in one take)

1. Face or title — 5s
2. Matrix Zone A/B — 25s
3. Standard clauses — 20s
4. Terminal unittest + harness — 25s
5. Digest JSON — 10s
6. Closing card — 10s

## Cuts if you run long

Drop: the 20 million COP simulation sentence.
Keep: three zones, kill switch, unowned harness, correlated-did-not-page, step-two A1, digest, "does not certify any lab."
